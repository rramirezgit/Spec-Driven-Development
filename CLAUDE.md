## Git Rules

- Never include "Co-Authored-By" lines in commits
- Never include "Generated with Claude Code" or any Claude/AI mention in PR descriptions, commit messages, or comments
- All git operations must use the project's local git config identity
